#ifndef SNAPSHOT
#define SNAPSHOT

#include <iostream>

class Snapshot {
    public:
        std::string info;
        int value;
        Snapshot(std::string info, int value);
};

#endif // SNAPSHOT
